---
title: "GET /v1/embed/macro/series/{iso2}/{indicator}"
description: "Public full-history series and stats for one country indicator"
---

Public full-history series and stats for one country indicator

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/embed/macro/series/example-iso2/example-indicator"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/embed/macro/series/{iso2}/{indicator} to public full-history series and stats for one country indicator. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
