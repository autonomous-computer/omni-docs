---
title: "GET /v1/embed/macro/countries/{iso2}"
description: "Public latest values across the macro-map indicators for one country"
---

Public latest values across the macro-map indicators for one country

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/embed/macro/countries/example-iso2"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/embed/macro/countries/{iso2} to public latest values across the macro-map indicators for one country. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
