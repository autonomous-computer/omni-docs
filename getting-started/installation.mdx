---
title: Installation
description: Set up Omni docs and development services locally.
---

# Installation

This setup mirrors the active `omni-apps` monorepo workflow.

## Prerequisites

- Node.js 20+
- pnpm 8+
- Docker (recommended for local services)
- PostgreSQL 16+ (for full backend features)

## Clone and install

```bash
# omni-apps monorepo
git clone https://github.com/autonomous-computer/omni-apps.git
cd omni-apps
pnpm install
```

## Configure environment

```bash
cp apps/agent-backend/.env.example apps/agent-backend/.env
# Fill required secrets and API keys
```

## Run services

```bash
# Web app
pnpm --filter @omni/omni-web dev

# Agent backend
pnpm --filter @omni/agent-backend dev

# Optional desktop shell
pnpm --filter @omni/omni-desktop dev
```

## Verify

- Web: `http://localhost:3000`
- Backend health: `http://localhost:3001/health`

## Recommended next reads

- [/core-concepts/conversations](/core-concepts/conversations)
- [/sources/overview](/sources/overview)
- [/skills/overview](/skills/overview)
